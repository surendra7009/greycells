class GCPackageInfo {
  String? appName;
  String? packageName;
  String? version;
  String? buildNumber;

  GCPackageInfo(
      {this.buildNumber, this.version, this.packageName, this.appName});
}
