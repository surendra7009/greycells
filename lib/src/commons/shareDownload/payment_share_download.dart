import 'package:flutter/material.dart';

class PaymentShareDownload extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: <Widget>[
          MaterialButton(
            onPressed: () => null,
          )
        ],
      ),
    );
  }
}
